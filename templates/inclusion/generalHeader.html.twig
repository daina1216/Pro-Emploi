<nav class="navbar navbar-expand-lg my-navbar">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">
      Pro_Emploi

    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation" >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
        <li class="nav-item" role="presentation">

          {% if app.user.status =="Recruteur" %}
            
            <a class="nav-link my-link" aria-current="page" href="{{path('homePage')}}">
              <i class="my-icone bi bi-house-door"></i>
              Mes Offres
            </a>

          {% else %}

            <a class="nav-link my-link" aria-current="page" href="{{path('homePage')}}">
              <i class="my-icone bi bi-house-door"></i>
               Les offres d'emploi
            </a>

          {% endif %}
          
        </li>
        <li class="nav-item" role="presentation">

          {% if app.user.status =="Recruteur" %}
            
            <a class="nav-link my-link" aria-current="page" href="{{path('createJob')}}">
              <i class="my-icone bi bi-house-door"></i>
              Créer une offre d'emploi
            </a>

          {% else %}

            <a class="nav-link my-link" aria-current="page" href="{{path('postulerNotif')}}">
              <i class="my-icone bi bi-house-door"></i>
               Mes candidatures
            </a>

          {% endif %}

        </li>

      </ul>
      <ul class="navbar-nav ml-auto">
      
        <li class="nav-item dropdown">

          {% if app.user and app.user.status=="Recruteur" %}

              <a class="nav-link dropdown-toggle text-light" data-bs-toggle="dropdown" href="#" id="compteDropdownLink">
                <img src="{{ asset('assets/img/im.jpg')}}" class="img-circle mr-1" height="25" width="25" style="border-radius: 50%">
                <span class="brand-text text-light"> {{ app.user.nom }} {{app.user.prenoms}} </span>
              </a>

              <div class="dropdown-menu  dropdown-menu-right">
                <a href="{{path('postulerShow')}}" class="dropdown-item">
                  <i class="fa fa-pencil-square-o mr-2" aria-hidden="true"></i> Notications
                </a>

                <a href="{{ path('app_logout')}}" class="dropdown-item">
                  <i class="fa fa-low-vision mr-2" aria-hidden="true"></i> Déconnexion
                </a>
                
              </div>

          {% else %}

             <a class="nav-link dropdown-toggle text-light" data-bs-toggle="dropdown" href="#" id="compteDropdownLink">
                <img src="{{ asset('assets/img/im.jpg')}}" class="img-circle mr-1" height="25" width="25" style="border-radius: 50%">
                <span class="brand-text text-light"> {{ app.user.nom }} {{app.user.prenoms}} </span>
              </a>

              <div class="dropdown-menu  dropdown-menu-right">
                <a href="{{path('postulerIndex')}}" class="dropdown-item">
                  <i class="fa fa-pencil-square-o mr-2" aria-hidden="true"></i> Mes offres
                </a>
                <a href="{{path('postulerNotif')}}" class="dropdown-item">
                  <i class="fa fa-pencil-square-o mr-2" aria-hidden="true"></i> Notifications
                </a>

                <a href="{{ path('app_logout')}}" class="dropdown-item">
                  <i class="fa fa-low-vision mr-2" aria-hidden="true"></i> Déconnexion
                </a>
                
              </div>

          {% endif %}
        </li>

      </ul>

    </div>
  </div>
</nav>